name: CI Pipeline

on:
  push:
    branches: [ "main", "fitur-biodata" ]
  pull_request:
    branches: [ "main", "fitur-biodata" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Say hello
        run: echo "Pipeline CI pertama saya berhasil berjalan!"
